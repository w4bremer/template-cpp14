{% assign module_id = module.name|identifier -%}
{% for interface in module.interfaces %}
#include "{{module_id}}/implementation/{{interface.name|lower }}.h"
{% endfor %}

using namespace {{ system.name | capital }}::{{ module.name | capital }};

int main(){
    {% for interface in module.interfaces %}
    {% assign class = interface.name | capital %}
    std::unique_ptr<I{{class}}> test{{class}} = std::make_unique<{{class}}>();
    {% endfor %}

    return 0;
}
