{% comment %} // Copyright (c) ApiGear UG 2020 {% endcomment -%}
#pragma once

#if defined _WIN32 || defined __CYGWIN__
#ifdef __GNUC__
  #define {{ system.name | constant }}_{{ module.name | constant }}_EXPORT __attribute__ ((dllexport))
#else
  #define {{ system.name | constant }}_{{ module.name | constant }}_EXPORT __declspec(dllexport)
#endif
#else
  #if __GNUC__ >= 4
    #define {{ system.name | constant }}_{{ module.name | constant }}_EXPORT __attribute__ ((visibility ("default")))
  #else
    #define {{ system.name | constant }}_{{ module.name | constant }}_EXPORT
  #endif
#endif
